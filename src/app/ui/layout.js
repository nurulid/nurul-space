import ContentLayout from "@/components/template/ContentLayout";

export const metadata = {
  title: "UI | Nurul's website",
  keyword: "UI, user interface, UI website, UI/UX, nurul insani dewi, nurul insani, nurul, nurul insan, nurul id, nurulid, nurulide"
};

export default function layout({ children }) {
  return <ContentLayout>{children}</ContentLayout>;
}
